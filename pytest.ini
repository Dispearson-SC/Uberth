[pytest]
testpaths = tests
markers =
    slow: exercises multiple full-shift generations and/or the ~120MB OSMnx
        drive-graph fixture. Deselect for a fast dev loop with -m "not slow".
addopts = -ra
